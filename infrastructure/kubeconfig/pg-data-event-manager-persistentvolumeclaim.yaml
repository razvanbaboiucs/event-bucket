apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: pg-data-event-manager
  name: pg-data-event-manager
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
