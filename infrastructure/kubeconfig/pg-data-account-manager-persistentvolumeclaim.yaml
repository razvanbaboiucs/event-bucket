apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: pg-data-account-manager
  name: pg-data-account-manager
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
